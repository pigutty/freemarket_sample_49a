.product-buy-confirm
  =render "shared/new-header"
  =render "shared/confirm-product", name: @product.name,price:@product.price
  -# =render "shared/confirm-product", name: @product.name,price:@product.price,image_url:@product.image
  =render "shared/new-footer"
